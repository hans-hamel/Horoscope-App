<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.HoroscopeApp" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:statusBarColor">@color/secondary</item>
    </style>

    <style name="Theme.HoroscopeApp" parent="Base.Theme.HoroscopeApp" />

    <style name="Theme.HoroscopeApp.Text" parent="@android:style/TextAppearance">
        <item name="android:textColor">@color/secondary</item>
        <item name="android:textStyle">bold</item>
        <item name="android:fontFamily">@font/dancing</item>
    </style>

    <style name="Theme.ActionBar.Title" parent="Theme.HoroscopeApp.Text">
        <item name="android:textSize">30sp</item>
    </style>

    <style name="HoroscopeDetailBody" parent="Theme.HoroscopeApp.Text">
        <item name="android:textStyle">normal</item>
        <item name="android:textSize">20sp</item>
    </style>
</resources>